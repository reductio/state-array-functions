{
  "name": "state-array-functions",
  "version": "0.0.2",
  "description": "Filter and map functions for arrays used in states where objects/arrays are not altered but replaced when changed, e.g. redux states.",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && rollup -c && cp package.json LICENSE README.md dist/",
    "test": "jest"
  },
  "keywords": [
    "array",
    "state",
    "redux",
    "map",
    "filter"
  ],
  "author": "Janko Timmermann",
  "repository": "reductio/state-array-functions",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.11.4",
    "@babel/preset-env": "^7.11.0",
    "@babel/preset-typescript": "^7.10.4",
    "@rollup/plugin-typescript": "^5.0.2",
    "@types/jest": "^26.0.10",
    "babel-jest": "^26.3.0",
    "jest": "^26.4.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.26.8",
    "rollup-plugin-terser": "^7.0.1",
    "rollup-plugin-typescript2": "^0.27.2",
    "tslib": "^2.0.1",
    "typescript": "^4.0.2"
  }
}
