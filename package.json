{
  "name": "state-array-functions",
  "version": "0.0.1",
  "description": "Functions for arrays used in states where objects are not altered but replaced when changed, e.g. redux states.",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "test": "yarn build && jest"
  },
  "keywords": [
    "array",
    "state",
    "redux",
    "map",
    "filter"
  ],
  "author": "Janko Timmermann",
  "license": "MIT",
  "devDependencies": {
    "jest": "^26.4.2",
    "rollup": "^2.26.8",
    "rollup-plugin-terser": "^7.0.1"
  }
}
